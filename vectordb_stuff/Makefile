

help:
	cargo run -- --help

fmt:
	cargo fmt

test:
	cargo test

build: fmt
	cargo build

run: build
	cargo run

test_clear_database:
	cargo run -- --clear-database

test_load_documents:
	cargo run -- --load-documents
