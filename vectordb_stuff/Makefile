

help:
	cargo run -- --help

fmt:
	cargo fmt

test:
	cargo test

build: fmt
	cargo build

run: build
	cargo run

clear_database:
	cargo run -- --clear-database

load_documents:
	cargo run -- --load-documents
